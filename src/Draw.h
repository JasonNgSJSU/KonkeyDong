#pragma once

#include "Player.h"
#include "Camera.h"
#include "Star.h"
#include "Enemy.h"

//static void playerDraw(Player* player, Camera * camera);
//static void enemyDraw(Enemy* enemy, Camera * camera);
//static void starDraw(Star* s, Camera * camera);